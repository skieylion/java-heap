package project.java;

import java.io.FileInputStream;
import java.io.FileNotFoundException;
import java.io.IOException;
import java.nio.charset.StandardCharsets;
import java.util.Base64;
import java.util.Spliterator;
import java.util.concurrent.Semaphore;

public class App {

    public static void main(String[] args) {
//        String path = App.class.getClassLoader().getResource("base64.txt").getPath();
//        try (var is = new FileInputStream(path)) {
//            byte[] data = is.readAllBytes();
//            var bytes = Base64.getDecoder().getMimeDecoder().decode(new String(data, StandardCharsets.UTF_8));
//            var string = new String(bytes, StandardCharsets.UTF_8);
//            System.out.println(string);
//        } catch (IOException e) {
//            e.printStackTrace();
//        }


    }
}
